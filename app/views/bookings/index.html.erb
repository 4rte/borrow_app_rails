<div class="container">
  <h1>All my Bookings</h1>
  <% @bookings.each do |booking|%>
    <p><%= booking.tool.name %></p>
    <p><%= booking.tool.category %></p>
    <p><%= booking.start_date %></p>
    <p><%= booking.end_date %></p>
  <% end %>

  <%= link_to 'Back to Homepage', root_path %>
</div>
