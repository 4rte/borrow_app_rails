
<div class="container">
  <h1>All tools</h1>
<div class="row">

  <% @tools.each do |tool| %>
  <div class="col-12 col-md-6 col-lg-3">
    <div class="ca"><%= link_to tool.name, tool_path(tool) %> | <%= tool.category %> | <%= tool.description %> </ul>
  </li>
  <% end %>
  </div>
    </div>
  <% if current_user  %>
    <%= link_to 'Add a new tool', new_tool_path  %>
  <% end %>
</div>

</div>
